{
    "name": "global-infra",
    "main": "index.ts",
    "devDependencies": {
        "@types/node": "^14"
    },
    "dependencies": {
        "@pulumi/pulumi": "^3.0.0",
        "@pulumi/aws": "^5.0.0",
        "@pulumi/awsx": "^0.40.0"
    },
    "scripts": {
        "stack-up": "pulumi up",
        "stack-deploy": "pulumi up --skip-preview -y"
    }
}
